(()=>{
const $=id=>document.getElementById(id),n=v=>Number(v||0),fmt=(v,d=2)=>n(v).toLocaleString('en-US',{minimumFractionDigits:d,maximumFractionDigits:d});
const compact=v=>{v=n(v);if(v>=1e12)return(v/1e12).toFixed(2)+'T';if(v>=1e9)return(v/1e9).toFixed(2)+'B';if(v>=1e6)return(v/1e6).toFixed(2)+'M';if(v>=1e3)return(v/1e3).toFixed(2)+'K';return fmt(v)};
const hash=v=>{v=n(v);if(v>=1e15)return(v/1e15).toFixed(2)+' P/s';if(v>=1e12)return(v/1e12).toFixed(2)+' T/s';if(v>=1e9)return(v/1e9).toFixed(2)+' G/s';if(v>=1e6)return(v/1e6).toFixed(2)+' M/s';if(v>=1e3)return(v/1e3).toFixed(2)+' K/s';return fmt(v)+' H/s'};
const coin=v=>n(v).toFixed(8)+' FIX';const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));const set=(id,v)=>{const e=$(id);if(e)e.textContent=v};
let filter='all',events=[],lastData=null;
function duration(sec){sec=Math.max(0,Math.floor(sec));return String(Math.floor(sec/60)).padStart(2,'0')+':'+String(sec%60).padStart(2,'0')}
function roundLeft(start,target){if(!start)return 0;const t=Date.parse(String(start).replace(' ','T')+'Z')/1000;return Math.max(0,target-(Date.now()/1000-t))}
function eta(hashrate,diff){if(hashrate<=0||diff<=0)return'—';const seconds=diff*4294967296/hashrate;if(seconds<60)return`~${Math.ceil(seconds)}s`;if(seconds<3600)return`~${Math.round(seconds/60)}m`;if(seconds<86400)return`~${(seconds/3600).toFixed(1)}h`;const d=Math.floor(seconds/86400),h=Math.round((seconds%86400)/3600);return`~${d}d ${h}h`}
function pct(a,b){return b?Math.min(100,a/b*100):0}
function workers(d){const ws=d.mining?.workers||{},a=Object.entries(ws),shares=d.shares||[];const online=a.filter(([,w])=>w.active).length;set('workerPill',`${online} ONLINE`);if(!a.length){$('workerList').innerHTML='<div class="empty">No authorized miners seen yet.</div>';return}
$('workerList').innerHTML=a.map(([name,w])=>{const mine=shares.filter(s=>String(s.worker||'')===name);const last=mine.length?mine[mine.length-1].epoch:0;const ago=last?`${Math.max(0,Math.floor(Date.now()/1000-last))}s ago`:'—';return`<div class="miner-row ${w.active?'':'off'}"><span class="miner-name"><i class="led"></i>${esc(name)}</span><span>${compact(w.difficulty)}</span><span>${n(w.accepted)}</span><span>${n(w.rejected)}</span><span>${ago}</span><span class="miner-status">${w.active?'ONLINE':'IDLE'}</span></div>`}).join('')}
function blocks(d){const list=d.blocks||[],height=n(d.node?.height);set('blocksPill',`${list.length} FOUND`);if(!list.length){$('blockList').innerHTML='<div class="empty">No solo blocks found yet.</div>';return}
$('blockList').innerHTML=list.slice(0,20).map(b=>{const bh=n(b.height),conf=n(b.confirmations),rem=n(b.maturity_remaining),state=String(b.state||'PENDING').toLowerCase();const valid=bh&&height?bh<=height:true;const validity=valid?'VALID':'STALE';const mat=state==='matured'?'MATURE':state==='immature'?`${rem} LEFT`:'PENDING';return`<div class="block-row"><span class="block-height">${bh?fmt(bh,0):'—'}</span><span class="block-hash" title="${esc(b.blockhash||b.txid||'')}">${esc((b.blockhash||b.txid||'').slice(0,20))}${(b.blockhash||b.txid)?'…':''}</span><span class="block-reward">${coin(b.reward)}</span><span>${fmt(conf,0)}</span><span>${mat}</span><span><b class="state-badge ${state}">${state}</b><small class="validity ${validity==='STALE'?'expired':''}">${validity}</small></span></div>`}).join('')}
function chart(d){const a=(d.history_diff||[]).slice(-180);if(a.length<2)return;const v=a.map(x=>n(x.difficulty)),lo=Math.min(...v),hi=Math.max(...v),span=hi-lo||1;const p=v.map((x,i)=>`${i/(v.length-1)*800},${174-(x-lo)/span*150}`).join(' ');const line=$('diffLine');if(line)line.setAttribute('points',p)}
function eventType(e){const s=String(e.message||'');if(/ACCEPT/.test(s))return'ACCEPT';if(/REJECT|ERROR/.test(s))return'ERROR';if(/NEW ROUND|Job /.test(s))return'NEW ROUND';if(/WARN|failed/i.test(s))return'WARN';return'INFO'}
function eventWorker(s){const m=s.match(/(?:authorize|worker=)([^\s,]+)/i);return m?m[1]:s.match(/ACCEPT.*?\s([a-zA-Z0-9_.:+-]+)\s+work=/)?.[1]||'—'}
function eventData(e){const s=String(e.message||'');const work=s.match(/work=([0-9.]+)/i)?.[1]||'—';const diff=s.match(/(?:pool|diff)=([0-9.]+)/i)?.[1]||'—';const hashv=s.match(/hash=([0-9a-f]+)/i)?.[1]||'—';const eff=s.match(/round=([0-9.]+)%/i)?.[1]||'—';return{type:eventType(e),worker:eventWorker(s),work,diff,eff,hash:hashv}}
function showEvents(){let a=events;if(filter!=='all')a=a.filter(e=>{const s=String(e.message||'').toLowerCase();if(filter==='share')return /accept|reject|share/.test(s);if(filter==='job')return /job|round|notify/.test(s);if(filter==='block')return /block/.test(s);return /error|failed|warn/.test(s)});a=a.slice(-35).reverse();$('eventsList').innerHTML=a.length?a.map(e=>{const x=eventData(e),cl=x.type==='ACCEPT'?'accept':x.type==='NEW ROUND'?'round':x.type==='ERROR'?'error':x.type==='WARN'?'warn':'round';return`<div class="event-row"><span class="time">${esc(e.ts||'')}</span><span><b class="event-tag ${cl}">${x.type}</b></span><span title="${esc(x.worker)}">${esc(x.worker)}</span><span>work=${esc(x.work)}</span><span>diff=${esc(x.diff)}</span><span>effort=${esc(x.eff)}%</span><span class="event-hash">${esc(x.hash)}</span></div>`}).join(''):'<div class="empty">No matching events.</div>'}
function render(d){lastData=d;const m=d.mining||{},node=d.node||{},r=d.round||{},w=d.wallet||{};const target=n(node.difficulty||r.difficulty),best=n(m.best_share),p=pct(best,target);set('topHeight',fmt(node.height,0));set('topPeers',fmt(node.connections,0));set('topSync',node.synced?'100.00%':(n(node.verification_progress)*100).toFixed(2)+'%');set('topUpdate',new Date((d.last_update||Date.now()/1000)*1000).toLocaleTimeString('de-DE'));
set('difficulty',compact(target));set('shareDiff',compact(m.fixed_difficulty));set('hashrate',hash(m.hashrate_5m));set('hashrate1h',hash(m.hashrate_1h));set('networkShare',node.network_hashrate?((n(m.hashrate_5m)/n(node.network_hashrate))*100).toFixed(3)+'% of network':'—');set('bestMetric',compact(best));set('bestMetricPct',p.toFixed(3)+'% of target');set('bestLarge',compact(best));set('targetLarge',compact(target));set('remaining',compact(Math.max(0,target-best))+' remaining');set('targetPct',p.toFixed(3)+'%');$('targetBar').style.width=p+'%';set('roundPill',`ROUND #${fmt(r.height||node.height,0)}`);set('roundNumber','#'+fmt(r.height||node.height,0));
set('roundWork',compact(r.work));set('accepted',fmt(m.accepted,0));set('rejected',fmt(m.rejected,0));set('rejectRate',fmt(m.reject_pct,2)+'%');set('totalShares',fmt(m.accepted,0));set('acceptedTrend',`▲ ${r.shares?((n(r.shares)/Math.max(1,n(m.accepted)))*100).toFixed(1):'0.0'}%`);set('rejectedTrend',fmt(m.reject_pct,2)+'%');set('eta',eta(n(m.hashrate_5m),target));set('luck',m.round_effort?`${(100/Math.max(.0001,n(m.round_effort))).toFixed(1)}%`:'—');
set('target',node.target||'—');set('bits',node.bits||'—');set('netHash',hash(node.network_hashrate));set('peers',fmt(node.connections,0));set('core',node.online?'ONLINE':'OFFLINE');set('sync',node.synced?'SYNCED':'SYNCING');set('chain',node.chain||'—');set('verify',(n(node.verification_progress)*100).toFixed(2)+'%');set('uptime','—');set('sideUptime','—');set('sideHeight','Height '+fmt(node.height,0));
const online=node.online;set('topNode',online?'ONLINE':'OFFLINE');$('topNode').className='status-chip '+(online?'green':'red');$('sideNode').className='side-status '+(online?'green':'red');$('sideNode').innerHTML=`<i class="dot"></i>${online?'ONLINE':'OFFLINE'}`;
const left=roundLeft(r.started_at,r.target_seconds||600),total=r.target_seconds||600,progress=total?Math.max(0,Math.min(100,(1-left/total)*100)):0;set('roundTimerValue',duration(left));set('roundTimerState',left?'ROUND ACTIVE':'ROUND ELAPSED');$('roundTrack').style.width=progress+'%';
set('walletConfirmed',coin(w.confirmed));set('walletPending',coin(w.pending));set('walletImmature',coin(w.immature));set('walletTotal','Total wallet balance: '+coin(w.total));set('maturityValue',`${Math.max(0,n(w.immature)?100-n(w.blocks?.[0]?.confirmations||0):100)} / 100`);
workers(d);blocks(d);chart(d);showEvents()}
async function load(){try{const r=await fetch('/api/status',{cache:'no-store'});const d=await r.json();render(d);const l=await fetch('/api/logs',{cache:'no-store'});const j=await l.json();events=j.events||[];showEvents()}catch(e){set('topNode','OFFLINE');$('topNode').className='status-chip red';$('sideNode').className='side-status red';$('sideNode').innerHTML='<i class="dot"></i>OFFLINE'}}
document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;showEvents()}));
setInterval(()=>{const t=new Date();set('topUpdate',t.toLocaleTimeString('de-DE'));if(lastData)render(lastData)},1000);setInterval(load,3000);load();
})();