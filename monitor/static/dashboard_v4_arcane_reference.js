(()=>{'use strict';
if(window.__FIXEDCOIN_ARCANE_REFERENCE__)return;
window.__FIXEDCOIN_ARCANE_REFERENCE__=true;
const $=id=>document.getElementById(id);
const text=(id,v)=>{const e=$(id);if(e&&v!==undefined&&v!==null)e.textContent=String(v)};
const coin=v=>{const n=Number(v);return Number.isFinite(n)?n.toFixed(8)+' FIX':'0.00000000 FIX'};
const duration=s=>{s=Math.max(0,Math.floor(Number(s)||0));const d=Math.floor(s/86400),h=Math.floor(s%86400/3600),m=Math.floor(s%3600/60);return d?`${d}d ${String(h).padStart(2,'0')}h`:`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}`};
const first=(o,keys,def)=>{for(const k of keys){const v=k.split('.').reduce((a,x)=>a?.[x],o);if(v!==undefined&&v!==null&&v!=='')return v}return def};
function copyLive(){const workers=$('arcaneLiveWorkers')?.textContent||$('railWorkers')?.textContent||'—';const uptime=$('uptime')?.textContent||'—';const total=$('totalBalance')?.textContent||'—';text('arcaneLiveWorkers',workers);text('railWorkers',workers);text('railUptime',uptime);text('sidebarUptime',uptime);text('referenceBalance',total);text('railTime',new Date().toLocaleTimeString([], {hour12:false}))}
new MutationObserver(copyLive).observe(document.body,{subtree:true,childList:true,characterData:true});copyLive();
document.querySelectorAll('[data-scroll]').forEach(a=>a.addEventListener('click',e=>{const href=a.getAttribute('href');if(!href)return;const el=document.querySelector(href);if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth',block:'start'})}document.querySelectorAll('.arcane-nav a').forEach(x=>x.classList.remove('active'));a.classList.add('active')}));
const sections=[...document.querySelectorAll('.dashboard > section[id]')];
if('IntersectionObserver' in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){const target=e.target.id;document.querySelectorAll('.arcane-nav a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+target||(target==='forge'&&a.textContent.includes('Mining'))||(target==='blockHistory'&&a.textContent.includes('Blocks'))))}}),{rootMargin:'-35% 0px -55% 0px',threshold:0});sections.forEach(s=>io.observe(s))}
async function arcaneTelemetry(){try{const r=await fetch('/api/status?arcane='+Date.now(),{cache:'no-store'});if(!r.ok)return;const s=await r.json(),w=s.wallet||{},m=s.mining||{},n=s.node||{},round=s.round||{};const workers=Object.keys(m.workers||{}).length;if(workers)text('arcaneLiveWorkers',workers);text('railWorkers',workers||$('arcaneLiveWorkers')?.textContent||'—');const luck=first(s,['round.luck','mining.luck','luck'],100);const luckNum=Number(luck);if(Number.isFinite(luckNum))text('networkLuck',luckNum.toFixed(1)+'%');text('referenceBalance',coin(w.total));const estimated=first(w,['estimated_earnings','estimated','estimatedRewards'],null),pending=first(w,['pending_payout','pending','unconfirmed'],null),paid=first(w,['total_paid','paid','rewards'],null);if(estimated!==null)text('railEstimated',coin(estimated));if(pending!==null)text('railPending',coin(pending));if(paid!==null)text('railPaid',coin(paid));const up=first(s,['node.uptime_seconds','uptime_seconds'],null);if(up!==null){const u=duration(up);text('railUptime',u);text('sidebarUptime',u)}text('railTime',new Date().toLocaleTimeString([], {hour12:false}));if(round.height)text('roundId','#'+Number(round.height).toLocaleString('en-US'))}catch(_){} }
arcaneTelemetry();setInterval(arcaneTelemetry,3000);
const tilt=el=>{if(!window.matchMedia('(pointer:fine)').matches)return;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--arc-tilt-x',(-y*5).toFixed(2)+'deg');el.style.setProperty('--arc-tilt-y',(x*7).toFixed(2)+'deg');el.classList.add('arc-tilting')},{passive:true});el.addEventListener('pointerleave',()=>{el.style.setProperty('--arc-tilt-x','0deg');el.style.setProperty('--arc-tilt-y','0deg');el.classList.remove('arc-tilting')},{passive:true})};
document.querySelectorAll('.forge-metric,.forge-counter,.rail-card,.reference-balance').forEach(tilt);
document.documentElement.dataset.arcaneReference='1';
})();